apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vera-drive
  namespace: vera
spec:
  ingressClassName: traefik
  rules:
    - host: drive.vera.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vera-drive
                port:
                  number: 80
