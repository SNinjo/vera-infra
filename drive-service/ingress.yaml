apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: drive-service
  namespace: vera
spec:
  ingressClassName: traefik
  rules:
    - host: drive.vera.sninjo.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: drive-service
                port:
                  number: 80
