apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: site
  namespace: vera
spec:
  ingressClassName: traefik
  rules:
    - host: vera.sninjo.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: site
                port:
                  number: 80
